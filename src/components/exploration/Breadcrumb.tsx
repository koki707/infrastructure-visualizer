export function Breadcrumb({ items, onBack, onHome, onNavigate }: { items: string[]; onBack: () => void; onHome: () => void; onNavigate: (index: number) => void }) { return <nav aria-label="現在地" className="flex flex-wrap items-center gap-2 text-xs text-slate-500"><button onClick={onHome} className="rounded px-1 py-1 text-slate-700 hover:text-cyan-700">ネットワーク</button>{items.map((item, index) => <span key={`${item}-${index}`} className="flex items-center gap-2"><span className="text-slate-400">›</span><button onClick={() => onNavigate(index)} className="text-slate-700 hover:text-cyan-700">{item}</button></span>)}<button onClick={onBack} className="ml-auto rounded border border-slate-300 px-3 py-1.5 text-slate-700 transition hover:border-cyan-500 hover:text-cyan-700">← 1つ前へ戻る</button></nav> }
