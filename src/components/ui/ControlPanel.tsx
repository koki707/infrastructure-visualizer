import type { SimulationDestination } from '../../types/network'

interface Props { destinationId: string; destinations: SimulationDestination[]; sending: boolean; paused: boolean; onDestinationChange: (id: string) => void; onSend: () => void; onPause: () => void; onResume: () => void }
export function ControlPanel({ destinationId, destinations, sending, paused, onDestinationChange, onSend, onPause, onResume }: Props) {
  const destination = destinations.find(item => item.id === destinationId) ?? destinations[0]
  return <section className="panel p-5"><p className="eyebrow">接続先を選ぶ</p><label className="mt-3 block text-sm text-slate-700" htmlFor="destination">シミュレーションするウェブサイト</label><select id="destination" value={destination.id} disabled={sending} onChange={(event) => onDestinationChange(event.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 text-sm font-semibold text-slate-800 outline-none transition focus:border-cyan-500 focus:ring-2 focus:ring-cyan-100 disabled:bg-slate-50 disabled:text-slate-500">{destinations.map(item => <option key={item.id} value={item.id}>{item.label}</option>)}</select><code className="mt-2 block break-all rounded-lg bg-slate-50 px-3 py-2 font-mono text-xs text-cyan-800">{destination.url}</code><p className="mt-2 text-xs leading-5 text-slate-500">接続先の代表例：{destination.description}</p><button onClick={onSend} disabled={sending} className="mt-4 w-full rounded-lg bg-cyan-600 px-4 py-3 font-bold text-white transition hover:bg-cyan-700 disabled:cursor-not-allowed disabled:opacity-50">シミュレーションを開始</button>{sending && <button onClick={paused ? onResume : onPause} className={`mt-2 w-full rounded-lg border px-4 py-2.5 text-sm font-bold transition ${paused ? 'border-cyan-600 bg-cyan-50 text-cyan-700 hover:bg-cyan-100' : 'border-amber-300 bg-amber-50 text-amber-800 hover:bg-amber-100'}`}>{paused ? '▶ 再開する' : 'Ⅱ 一時停止する'}</button>}<p className="mt-3 text-xs leading-relaxed text-slate-500">{paused ? 'シミュレーションを停止中です。再開すると同じ位置から続きます。' : '選択したURLへ実際にアクセスしたり、DNS問い合わせ・パケット送信を行ったりしません。'}</p></section>
}
